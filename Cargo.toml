[package]
name = "variyak"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
seq-macro = "0.2"

[dev-dependencies]
libc = "0.2"
